# Prisma database connection for Tably
# Example for local Postgres running via docker compose
# Update credentials and host to match your environment.
DATABASE_URL="postgresql://tably:tably@localhost:5432/tably?schema=public"
