// Prisma Client is provided by the shared package @tably/db,
// which must be generated from packages/db/prisma/schema.prisma.
// Run `npm run prisma:generate` at repo root when the schema changes.
import { prisma } from "@tably/db";
export { prisma };
